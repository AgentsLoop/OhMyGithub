name: OpenCode reusable pipeline

on:
  workflow_call:
    inputs:
      issue_number:
        description: Issue containing the OpenCode label
        required: true
        type: string
      request:
        description: Exact issue request text
        required: true
        type: string
      issue_title:
        description: Issue title for reporting
        required: true
        type: string
      labels_json:
        description: JSON array of issue label names
        required: false
        default: '[]'
        type: string
      sender:
        description: GitHub username that submitted the request
        required: true
        type: string
      runtime_ref:
        description: Approved central runtime commit matching this reusable workflow
        required: true
        type: string
      target_ref:
        description: Validated result base branch
        required: true
        type: string
      target_sha:
        description: Validated project commit
        required: true
        type: string
    secrets:
      OPENCODE_API_KEY:
        required: false
      OPENCODE_AUTH_JSON:
        required: false
      AGENTSWEB_SSH_PUBLIC_KEY:
        required: false
jobs:
  opencode:
    # Ubuntu is the default; the mac label opts this run into macOS.
    runs-on: ${{ contains(inputs.labels_json, '"mac"') && 'macos-latest' || 'ubuntu-latest' }}
    permissions:
      id-token: write
      contents: write
      issues: write
      actions: read
    env:
      SSH_NAME: opencode-issue-${{ inputs.issue_number }}-${{ github.run_id }}-ssh
      SSH_DIR: ${{ github.workspace }}/.opencode-ssh
      OPENCODE_WEB_PORT: 4096
      OPENCODE_WEB_DIR: ${{ github.workspace }}/.opencode-web
      PROJECT_PATH: ${{ vars.PROJECT_DIR || './' }}
      PROJECT_DIR: ${{ github.workspace }}/${{ vars.PROJECT_DIR || './' }}
      OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
      OPENCODE_AUTH_CONTENT: ${{ secrets.OPENCODE_AUTH_JSON }}
      OPENCODE_EXPERIMENTAL: "true"
      OPENCODE_EXPERIMENTAL_WORKSPACES: "true"
      OPENCODE_ENABLE_EXA: "true"
      OPENCODE_ENABLE_EXPERIMENTAL_MODELS: "true"
      OPENCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS: "true"
      OPENCODE_EXPERIMENTAL_WEBSOCKETS: "true"
      TARGET_OWNER: ${{ github.repository_owner }}
      TARGET_REPO: ${{ github.event.repository.name }}
      TARGET_REPOSITORY: ${{ github.repository }}
      GH_REPO: ${{ github.repository }}
      TARGET_REF: ${{ inputs.target_ref }}
      TARGET_SHA: ${{ inputs.target_sha }}
      RUNTIME_DIR: ${{ github.workspace }}/.omgithub-runtime
      APP_PORT: 3000
      BRANCH_NAME: opencode/${{ github.run_id }}
      AGENTSWEB_SSH_ENABLED: ${{ secrets.AGENTSWEB_SSH_PUBLIC_KEY != '' }}
      TRIGGER_ISSUE_NUMBER: ${{ inputs.issue_number }}
      COMMENT_BODY: ${{ inputs.request }}
      LABELS_JSON: ${{ inputs.labels_json }}
      TEST_REQUEST: ${{ contains(inputs.labels_json, '"test"') && contains(inputs.labels_json, '"OpenCode"') }}
      GOAL_REQUEST: ${{ contains(inputs.labels_json, '"Goal"') }}
      RALPH_REQUEST: ${{ contains(inputs.labels_json, '"ralph"') }}
      OMO_REQUEST: ${{ contains(inputs.labels_json, '"omo"') }}
      SSH_ONLY_REQUEST: ${{ contains(inputs.labels_json, '"ssh"') }}
      VALIDATION_ENABLED: ${{ vars.VALIDATION_ENABLED || 'true' }}
    steps:
      - name: Authenticate status callbacks
        continue-on-error: true
        shell: bash
        env:
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          OMGITHUB_ORIGIN: ${{ vars.OMGITHUB_ORIGIN || 'https://omgithub.com' }}
        run: |
          /usr/bin/time -p node --input-type=module <<'NODE'
          import { appendFileSync } from 'node:fs';
          const env = process.env;
          const origin = env.OMGITHUB_ORIGIN.replace(/\/$/, '');
          const url = new URL(env.ACTIONS_ID_TOKEN_REQUEST_URL);
          url.searchParams.set('audience', origin);
          async function json(url, options) {
            const response = await fetch(url, { ...options, signal: AbortSignal.timeout(15000) });
            if (!response.ok) throw new Error(`Callback authentication HTTP ${response.status}`);
            return response.json();
          }
          for (let attempt = 0; attempt < 3; attempt++) {
            try {
              const identity = await json(url, { headers: { authorization: `Bearer ${env.ACTIONS_ID_TOKEN_REQUEST_TOKEN}` } });
              const { token } = await json(`${origin}/api/github/${env.GITHUB_REPOSITORY}/issues/${env.ISSUE_NUMBER}/callback-token`, {
                method: 'POST', headers: { authorization: `Bearer ${identity.value}`, 'content-type': 'application/json' },
                body: JSON.stringify({ run: env.GITHUB_RUN_ID, attempt: Number(env.GITHUB_RUN_ATTEMPT) })
              });
              console.log(`::add-mask::${token}`);
              appendFileSync(env.GITHUB_ENV, `OMGITHUB_CALLBACK_TOKEN=${token}\n`);
              break;
            } catch (error) {
              if (attempt === 2) throw error;
              await new Promise(resolve => setTimeout(resolve, 2000 * 2 ** attempt));
            }
          }
          NODE
      - name: Register Actions progress
        continue-on-error: true
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          OMGITHUB_ORIGIN: ${{ vars.OMGITHUB_ORIGIN || 'https://omgithub.com' }}
        run: |
          set -euo pipefail
          # Bootstrap without the runtime checkout; the lifecycle owns later updates.
          /usr/bin/time -p curl --fail-with-body --silent --show-error --retry 3 --retry-all-errors --max-time 20 \
            -H "Authorization: Bearer $OMGITHUB_CALLBACK_TOKEN" -H 'Content-Type: application/json' \
            --data "{\"run\":\"$GITHUB_RUN_ID\",\"attempt\":$GITHUB_RUN_ATTEMPT,\"sequence\":1,\"state\":\"starting\"}" \
            "$OMGITHUB_ORIGIN/api/github/$GITHUB_REPOSITORY/issues/$TRIGGER_ISSUE_NUMBER/run"

      - name: Publish task link
        shell: bash
        run: |
          set -euo pipefail
          issue_url="https://github.com/${GITHUB_REPOSITORY}/issues/${TRIGGER_ISSUE_NUMBER}"
          {
            echo '## OpenCode task'
            echo
            echo "[Open triggering issue](${issue_url})"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Export target token
        shell: bash
        run: echo "GH_TOKEN=${{ github.token }}" >> "$GITHUB_ENV"

      - name: Checkout target repository
        uses: actions/checkout@v6
        with:
          repository: ${{ github.repository }}
          ref: ${{ inputs.target_sha }}
          token: ${{ github.token }}
          fetch-depth: 1
          persist-credentials: true

      - name: Checkout centralized OMG runtime
        uses: actions/checkout@v6
        with:
          repository: AgentsLoop/OhMyGithub
          ref: ${{ inputs.runtime_ref }}
          path: .omgithub-runtime
          token: ${{ github.token }}
          fetch-depth: 1
          persist-credentials: false

      - name: Prepare selected remix snapshot
        timeout-minutes: 15
        shell: bash
        run: /usr/bin/time -p node "$RUNTIME_DIR/scripts/remix-snapshot.mjs"

      - name: Load saved game checkpoint
        if: env.TEST_REQUEST != 'true' && env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        run: /usr/bin/time -p node "$RUNTIME_DIR/scripts/session-checkpoint.mjs" prepare

      - name: Configure Linux Playwright CLI with WebGPU
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/time -p bash "$RUNTIME_DIR/scripts/setup-linux-playwright.sh"

      - name: Checkout AgentsWeb tunnel client
        if: env.TEST_REQUEST != 'true'
        uses: actions/checkout@v6
        with:
          repository: agents-dev/agent-workspace
          path: .agentsweb
          sparse-checkout: |
            lolgames_tunnel
          persist-credentials: false

      - name: Resolve project directory
        shell: bash
        run: |
          set -euo pipefail
          if [[ "$TEST_REQUEST" == 'true' ]]; then
            PROJECT_PATH='workflow-test-project'
          fi
          if [[ "$PROJECT_PATH" = /* || "$PROJECT_PATH" =~ (^|/)\.\.(/|$) ]]; then
            echo "PROJECT_DIR must be a path inside the repository: $PROJECT_PATH" >&2
            exit 1
          fi
          project_path="${PROJECT_PATH#./}"
          project_path="${project_path%/}"
          project_path="${project_path:-.}"
          if [[ "$project_path" == '.' ]]; then
            resolved_project_dir="$GITHUB_WORKSPACE"
          else
            resolved_project_dir="$GITHUB_WORKSPACE/$project_path"
          fi
          printf 'PROJECT_PATH=%s\n' "$project_path" >> "$GITHUB_ENV"
          printf 'PROJECT_DIR=%s\n' "$resolved_project_dir" >> "$GITHUB_ENV"

      - name: Ignore generated project files before OpenCode
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/time -p mkdir -p "$PROJECT_DIR"
          /usr/bin/time -p touch "$PROJECT_DIR/.gitignore"
          for pattern in 'screenshots/' '*.log' '*.pid' '.agents/' '.agentsweb/' \
            '.omgithub-runtime/' '.opencode-ssh/' '.opencode-web/' 'node_modules/'; do
            if ! /usr/bin/time -p grep -qxF "$pattern" "$PROJECT_DIR/.gitignore"; then
              /usr/bin/time -p printf '\n%s\n' "$pattern" >> "$PROJECT_DIR/.gitignore"
            fi
          done

      - name: Checkout shared OpenCode skills
        if: env.TEST_REQUEST != 'true'
        uses: actions/checkout@v6
        with:
          repository: agents-dev/skills
          path: ${{ env.PROJECT_PATH }}/.agents
          fetch-depth: 1
          persist-credentials: false

      - name: Exclude shared skills from generated changes
        shell: bash
        run: |
          set -euo pipefail
          project_path="${PROJECT_PATH#./}"
          project_path="${project_path%/}"
          [[ "$project_path" != '.' ]] || project_path=''
          skills_path="${project_path:+/$project_path}/.agents/"
          ralph_state_path="${project_path:+/$project_path}/.opencode/ralph-loop.local.md"
          screenshots_path="${project_path:+/$project_path}/screenshots/"
          grep -qxF "$skills_path" .git/info/exclude || echo "$skills_path" >> .git/info/exclude
          grep -qxF "$ralph_state_path" .git/info/exclude || echo "$ralph_state_path" >> .git/info/exclude
          grep -qxF "$screenshots_path" .git/info/exclude || echo "$screenshots_path" >> .git/info/exclude
          for pattern in '*.log' '*.pid'; do
            grep -qxF "$pattern" .git/info/exclude || echo "$pattern" >> .git/info/exclude
          done
          for relative_runtime_path in '.agentsweb/' '.omgithub-runtime/' '.opencode/goals/' \
            '.opencode-ssh/' '.opencode-web/' 'opencode-agentsweb-id_ed25519' \
            'opencode-agentsweb-id_ed25519.pub'; do
            runtime_path="${project_path:+/$project_path}/$relative_runtime_path"
            grep -qxF "$runtime_path" .git/info/exclude || echo "$runtime_path" >> .git/info/exclude
          done

      - name: Install worker instructions
        if: env.TEST_REQUEST != 'true'
        shell: bash
        run: |
          set -euo pipefail
          install -d "$PROJECT_DIR"
          template_dir="$RUNTIME_DIR/.github/templates"
          base_template="$template_dir/agents.template.md"
          echo "Loading worker templates from $template_dir for labels: $LABELS_JSON"
          [[ -f "$base_template" ]] || { echo "Missing worker base template: $base_template" >&2; exit 1; }
          cp "$base_template" "$PROJECT_DIR/Agents.md"

          append_template() {
            local template="$1"
            [[ -f "$template" ]] || { echo "Missing worker label template: $template" >&2; exit 1; }
            printf '\n' >> "$PROJECT_DIR/Agents.md"
            cat "$template" >> "$PROJECT_DIR/Agents.md"
          }

          # Match issue labels and request hashtags to template stems generically.
          template_keys="$(node -e '
            const labels = JSON.parse(process.env.LABELS_JSON || "[]");
            const tags = [...(process.env.COMMENT_BODY || "").matchAll(/(?:^|[^A-Za-z0-9_])#([A-Za-z0-9_-]+)/g)].map(match => match[1]);
            const keys = [...labels, ...tags].map(key => key.trim().toLowerCase());
            const platform = keys.includes("mac") ? "mac" : "linux";
            process.stdout.write(JSON.stringify([...keys.filter(key => key !== "mac" && key !== "linux"), platform]));
          ')"
          while IFS= read -r template; do
            template_name="${template##*/}"
            template_stem="${template_name%.md}"
            if jq -e --arg name "$template_stem" \
              'any(.[]; ((ascii_downcase | gsub("^[[:space:]]+|[[:space:]]+$"; "")) == ($name | ascii_downcase)))' \
              <<< "$template_keys" >/dev/null; then
              append_template "$template"
            fi
          done < <(find "$template_dir" -maxdepth 1 -type f -name '*.md' ! -name 'agents.template.md' -print | sort)

      - name: Sync OpenCode labels
        uses: actions/github-script@v7
        env:
          OPENCODE_MODEL: opencode/muse-spark-1.3-contributor-free
        with:
          github-token: ${{ github.token }}
          script: |
            const fs = require('node:fs');
            const path = require('node:path');

            const catalog = await fetch('https://models.opencode.ai/api.json').then(async (response) => {
              if (!response.ok) throw new Error(`OpenCode model catalog returned ${response.status}`);
              return response.json();
            });
            const models = Object.values(catalog.opencode?.models ?? {})
              .filter((model) =>
                (model.cost?.input === 0 && model.cost?.output === 0 && model.cost?.cache_read === 0) ||
                model.id === 'gpt-5.6-luna'
              )
              .map((model) => model.id)
              .filter(Boolean)
              .sort();
            if (!models.includes(process.env.OPENCODE_MODEL.slice('opencode/'.length))) {
              throw new Error(`Configured model is missing from the OpenCode catalog: ${process.env.OPENCODE_MODEL}`);
            }
            const openaiLuna = catalog.openai?.models?.['gpt-5.6-luna'];
            if (!openaiLuna) {
              throw new Error('OpenAI catalog is missing gpt-5.6-luna');
            }

            const skillsRoot = path.join(process.env.PROJECT_DIR, '.agents', 'skills');
            const skillNames = fs.existsSync(skillsRoot)
              ? fs.readdirSync(skillsRoot, { withFileTypes: true })
                  .filter((entry) => entry.isDirectory())
                  .filter((entry) => fs.existsSync(path.join(skillsRoot, entry.name, 'SKILL.md')))
                  .map((entry) => entry.name)
              : [];

            const labels = [
              { name: 'in progress', color: 'FBCA04', description: 'OpenCode workflow is running' },
              { name: 'validating', color: '1D76DB', description: 'OpenCode workflow is validating the result' },
              { name: 'complete', color: '0E8A16', description: 'OpenCode workflow completed successfully' },
              { name: 'failed', color: 'B60205', description: 'OpenCode workflow failed' },
              { name: 'Goal', color: '8A2BE2', description: 'Run OMG with the persistent Goal command' },
              { name: 'ralph', color: '5319E7', description: 'Run OpenCode with the Ralph auto-continuation loop' },
              { name: 'omo', color: '5319E7', description: 'Preinstall oh-my-openagent for this OpenCode run' },
              { name: 'test', color: '1D76DB', description: 'Run the full workflow with a mock OpenCode-generated project' },
              { name: 'mac', color: 'B60205', description: 'Run OpenCode on the macos-latest GitHub-hosted runner' },
              { name: 'linux', color: '1D76DB', description: 'Run OpenCode on the ubuntu-latest GitHub-hosted runner' },
              { name: 'ssh', color: '5319E7', description: 'Start an SSH-only debugging session without OpenCode' },
              ...models.map((model) => ({
                name: `model/opencode/${model}`,
                color: '5319E7',
                description: `OpenCode model: opencode/${model}`
              })),
              {
                name: 'model/openai/gpt-5.6-luna',
                color: '5319E7',
                description: 'OpenAI model: openai/gpt-5.6-luna'
              },
              ...skillNames.map((name) => ({
                name: `skill/${name}`,
                color: '1D76DB',
                description: `OpenCode skill: ${name}`
              }))
            ];

            for (const label of labels) {
              try {
                await github.rest.issues.createLabel({
                  owner: process.env.TARGET_OWNER,
                  repo: process.env.TARGET_REPO,
                  ...label
                });
              } catch (error) {
                if (error.status !== 422) throw error;
                await github.rest.issues.updateLabel({
                  owner: process.env.TARGET_OWNER,
                  repo: process.env.TARGET_REPO,
                  name: label.name,
                  color: label.color,
                  description: label.description
                });
              }
            }

            const managedPrefixes = ['model/', 'skill/'];
            const defaultLabels = new Set([
              'bug', 'documentation', 'duplicate', 'enhancement', 'good first issue',
              'help wanted', 'invalid', 'question', 'wontfix'
            ]);
            const existing = await github.paginate(github.rest.issues.listLabelsForRepo, {
              owner: process.env.TARGET_OWNER,
              repo: process.env.TARGET_REPO,
              per_page: 100
            });
            const desired = new Set(labels.map(({ name }) => name));
            for (const label of existing) {
              if (defaultLabels.has(label.name) ||
                  (managedPrefixes.some((prefix) => label.name.startsWith(prefix)) && !desired.has(label.name))) {
                await github.rest.issues.deleteLabel({
                  owner: process.env.TARGET_OWNER,
                  repo: process.env.TARGET_REPO,
                  name: label.name
                });
              }
            }

            core.info(`Synchronized ${labels.length} labels from the live OpenCode catalog and shared skills`);

      - name: Mark issue in progress
        continue-on-error: true
        uses: actions/github-script@v7
        env:
          GOAL_REQUEST: ${{ env.GOAL_REQUEST }}
          RALPH_REQUEST: ${{ env.RALPH_REQUEST }}
        with:
          retries: 3
          github-token: ${{ github.token }}
          script: |
            const fs = require('node:fs');
            const number = Number(process.env.TRIGGER_ISSUE_NUMBER);
            const statuses = ['in progress', 'validating', 'complete', 'failed'];
            for (const status of statuses) {
              try {
                await github.rest.issues.removeLabel({
                  owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
                  issue_number: number, name: status
                });
              } catch (error) {
                if (error.status !== 404) throw error;
              }
            }
            await github.rest.issues.addLabels({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: number,
              labels: [
                'in progress',
                ...(process.env.GOAL_REQUEST === 'true' ? ['Goal'] : []),
                ...(process.env.RALPH_REQUEST === 'true' ? ['ralph'] : [])
              ]
            });

      - name: Install preview process manager
        if: env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/time -p bash "$RUNTIME_DIR/scripts/setup-preview-runtime.sh"

      - name: Start AgentsWeb SSH session
        id: agentsweb
        if: env.TEST_REQUEST != 'true' && env.AGENTSWEB_SSH_ENABLED == 'true'
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          # GITHUB_RUN_NUMBER starts at 1 in every repository, so using it here
          # makes every new repository's first OMG run contend for port 32001.
          # GITHUB_RUN_ID is globally unique and distributes runs across the
          # broker's 32000-32999 public-port range.
          REQUESTED_SSH_PORT=$((32000 + GITHUB_RUN_ID % 1000))
          if [[ "$RUNNER_OS" == 'Linux' ]]; then
            sudo apt-get update
            sudo apt-get install -y openssh-server tmux
          else
            command -v tmux >/dev/null 2>&1 || brew install tmux
          fi
          install -d -m 700 "$SSH_DIR"
          SSH_KEY="$RUNNER_TEMP/opencode-agentsweb-id_ed25519"
          SSH_PUBLIC_KEY="$SSH_KEY.pub"
          printf 'SSH_KEY=%s\nSSH_PUBLIC_KEY=%s\n' "$SSH_KEY" "$SSH_PUBLIC_KEY" >> "$GITHUB_ENV"
          ssh-keygen -t ed25519 -f "$SSH_KEY" -N '' -q
          chmod 600 "$SSH_KEY"
          install -d -m 700 "$HOME/.ssh"
          {
            cat "$SSH_PUBLIC_KEY"
            printf '%s\n' '${{ secrets.AGENTSWEB_SSH_PUBLIC_KEY }}'
          } > "$HOME/.ssh/authorized_keys"
          chmod 600 "$HOME/.ssh/authorized_keys"
          [[ "$RUNNER_OS" != 'Linux' ]] || sudo mkdir -p /run/sshd
          # shellcheck disable=SC2024
          sudo /usr/sbin/sshd -D -e -p 2222 \
            -o PasswordAuthentication=no -o PermitRootLogin=no \
            -o PubkeyAuthentication=yes > "$SSH_DIR/sshd.log" 2>&1 &
          echo $! > "$SSH_DIR/sshd.pid"
          is_listening() {
            if [[ "$RUNNER_OS" == 'macOS' ]]; then
              if command -v nc >/dev/null 2>&1 && nc -z -w 1 127.0.0.1 2222 >/dev/null 2>&1; then
                return 0
              fi
              command -v lsof >/dev/null 2>&1 && sudo lsof -nP -a -iTCP:2222 -sTCP:LISTEN >/dev/null 2>&1
            elif command -v ss >/dev/null 2>&1; then
              ss -ltn | grep -q ':2222 '
            elif command -v lsof >/dev/null 2>&1; then
              lsof -nP -a -iTCP:2222 -sTCP:LISTEN >/dev/null 2>&1
            else
              nc -z 127.0.0.1 2222 >/dev/null 2>&1
            fi
          }
          for _ in {1..120}; do
            if is_listening; then
              break
            fi
            if ! kill -0 "$(cat "$SSH_DIR/sshd.pid")" 2>/dev/null; then
              echo 'Local sshd exited before listening on port 2222.' >&2
              sed -n '1,160p' "$SSH_DIR/sshd.log" >&2
              exit 1
            fi
            sleep 1
          done
          if ! is_listening; then
            echo 'Local sshd did not start listening on port 2222.' >&2
            sed -n '1,160p' "$SSH_DIR/sshd.log" >&2
            exit 1
          fi
          nohup env PYTHONPATH="$GITHUB_WORKSPACE/.agentsweb" \
            python3 -m lolgames_tunnel client 127.0.0.1:2222 \
            --server agentsweb.space --name "$SSH_NAME" --public-port "$REQUESTED_SSH_PORT" \
            > "$SSH_DIR/tunnel.log" 2>&1 &
          echo $! > "$SSH_DIR/tunnel.pid"
          for _ in {1..45}; do
            grep -q '^ssh://' "$SSH_DIR/tunnel.log" && break
            if ! kill -0 "$(cat "$SSH_DIR/tunnel.pid")" 2>/dev/null; then
              break
            fi
            sleep 1
          done
          if ! grep -q '^ssh://' "$SSH_DIR/tunnel.log"; then
            echo 'AgentsWeb tunnel did not register.' >&2
            sed -n '1,160p' "$SSH_DIR/tunnel.log" >&2
            exit 1
          fi
          SSH_TUNNEL_URL="$(sed -n 's#^\(ssh://[^[:space:]]*\).*#\1#p' "$SSH_DIR/tunnel.log" | head -n 1)"
          endpoint="$(sed -nE 's#^ssh://([^:[:space:]]+):([0-9]+).*$#\1 \2#p' "$SSH_DIR/tunnel.log" | head -n 1)"
          if [[ -z "$endpoint" ]]; then
            echo 'AgentsWeb tunnel registration did not contain a usable SSH endpoint.' >&2
            sed -n '1,160p' "$SSH_DIR/tunnel.log" >&2
            exit 1
          fi
          read -r SSH_HOST SSH_PORT <<<"$endpoint"
          [[ -n "$SSH_HOST" && "$SSH_PORT" =~ ^[0-9]+$ ]]
          SSH_COMMAND="ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i ~/.ssh/aiplay-agentsweb -p ${SSH_PORT} runner@${SSH_HOST}"
          {
            printf 'AGENTSWEB_HOST=%s\n' "$SSH_HOST"
            printf 'AGENTSWEB_PORT=%s\n' "$SSH_PORT"
            printf 'AGENTSWEB_COMMAND=%s\n' "$SSH_COMMAND"
            printf 'AGENTSWEB_TUNNEL_URL=%s\n' "$SSH_TUNNEL_URL"
          } >> "$GITHUB_ENV"
          {
            printf 'AGENTSWEB_HOST=%s\n' "$SSH_HOST"
            printf 'AGENTSWEB_PORT=%s\n' "$SSH_PORT"
            printf 'AGENTSWEB_COMMAND=%s\n' "$SSH_COMMAND"
            printf 'AGENTSWEB_TUNNEL_URL=%s\n' "$SSH_TUNNEL_URL"
          } > "$SSH_DIR/access.env"
          printf 'host=%s\nport=%s\ncommand=%s\n' "$SSH_HOST" "$SSH_PORT" "$SSH_COMMAND" >> "$GITHUB_OUTPUT"
          if [[ "$SSH_ONLY_REQUEST" == 'true' ]]; then
            ssh_verified=''
            for _ in {1..12}; do
              if ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
                -o BatchMode=yes -o ConnectTimeout=20 -o ConnectionAttempts=1 \
                -i "$SSH_KEY" -p "$SSH_PORT" "runner@$SSH_HOST" true; then
                ssh_verified='true'
                break
              fi
              sleep 5
            done
            if [[ "$ssh_verified" != 'true' ]]; then
              echo 'AgentsWeb registered, but the SSH endpoint is not reachable.' >&2
              sed -n '1,160p' "$SSH_DIR/tunnel.log" >&2
              exit 1
            fi
            body=$'🔐 **SSH-only debugging session is ready.**\n\n'
            body+="Run: https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"$'\n'
            body+="SSH URL: \`${SSH_TUNNEL_URL}\`"$'\n'
            body+="Host: \`${SSH_HOST}:${SSH_PORT}\`"$'\n\n'
            body+=$'Connect while the job is active:\n\n```sh\n'
            body+="${SSH_COMMAND}"$'\n```\n\n'
            body+=$'This run skips OpenCode and holds the runner for five hours for interactive debugging.'
            comment_response="$(jq -n --arg body "$body" '{body: $body}' \
              | gh api "repos/${TARGET_REPOSITORY}/issues/${TRIGGER_ISSUE_NUMBER}/comments" \
                --method POST --input -)"
            comment_id="$(jq -r '.id // empty' <<<"$comment_response")"
            [[ -n "$comment_id" ]]
            printf 'comment_id=%s\n' "$comment_id" >> "$GITHUB_OUTPUT"
          fi

      - name: Post SSH-only access details
        id: ssh_access_comment
        if: env.SSH_ONLY_REQUEST == 'true' && env.AGENTSWEB_SSH_ENABLED != 'true'
        uses: actions/github-script@v7
        env:
          AGENTSWEB_SSH_ENABLED: ${{ env.AGENTSWEB_SSH_ENABLED }}
        with:
          github-token: ${{ github.token }}
          script: |
            const number = Number(process.env.TRIGGER_ISSUE_NUMBER);
            const run = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const body = [
              '❌ **SSH-only debugging session could not start.**', '',
              `Run: ${run}`, 'The `AGENTSWEB_SSH_PUBLIC_KEY` secret is not configured.'
            ].join('\n');
            const comment = await github.rest.issues.createComment({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: number, body
            });
            core.setOutput('comment_id', comment.data.id);
            if (process.env.AGENTSWEB_SSH_ENABLED !== 'true') core.setFailed('AGENTSWEB SSH is required for the ssh label.');

      - name: Hold SSH-only debugging session
        if: env.SSH_ONLY_REQUEST == 'true' && env.AGENTSWEB_SSH_ENABLED == 'true' && steps.agentsweb.outcome == 'success'
        shell: bash
        run: |
          set -euo pipefail
          tunnel_pid="$(<"$SSH_DIR/tunnel.pid")"
          for _ in {1..18000}; do
            if ! kill -0 "$tunnel_pid" 2>/dev/null; then
              echo 'AgentsWeb tunnel exited during the SSH-only hold.' >&2
              exit 1
            fi
            sleep 1
          done

      - name: Report SSH-only startup failure
        id: ssh_failure_comment
        if: always() && env.SSH_ONLY_REQUEST == 'true' && env.AGENTSWEB_SSH_ENABLED == 'true' && steps.agentsweb.outcome != 'success'
        uses: actions/github-script@v7
        with:
          github-token: ${{ github.token }}
          script: |
            const number = Number(process.env.TRIGGER_ISSUE_NUMBER);
            const run = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const comment = await github.rest.issues.createComment({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: number,
              body: [
                '❌ **SSH-only debugging session could not start.**', '',
                `Run: ${run}`,
                'AgentsWeb registration or the SSH connectivity check failed. See the workflow logs for the tunnel output.'
              ].join('\n')
            });
            core.setOutput('comment_id', comment.data.id);
            core.setFailed('AgentsWeb registration or SSH connectivity check failed.');

      - name: Configure Mac runner DNS
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/time -p bash "$RUNTIME_DIR/scripts/setup-mac-dns.sh"

      - name: Configure Mac Playwright CLI with Metal
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/time -p bash "$RUNTIME_DIR/scripts/setup-mac-playwright.sh"

      - name: Start OpenCode web UI and Cloudflare tunnel
        id: opencode_web
        if: env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          set -euo pipefail
          install -d -m 700 "$OPENCODE_WEB_DIR"
          OPENCODE_VERSION="${RESUME_OPENCODE_VERSION:-}"
          if [[ -z "$OPENCODE_VERSION" ]]; then
          release_url="$(/usr/bin/time -p curl --fail --silent --show-error --location --retry 3 --max-time 30 \
            --output /dev/null --write-out '%{url_effective}' https://github.com/anomalyco/opencode/releases/latest)"
          OPENCODE_VERSION="${release_url##*/v}"
          fi
          [[ "$OPENCODE_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]
          curl --fail --silent --show-error --location https://opencode.ai/install \
            | bash -s -- --version "$OPENCODE_VERSION"
          OPENCODE_BIN="$HOME/.opencode/bin/opencode"
          [[ -x "$OPENCODE_BIN" ]]
          # The installer runs in a child shell; publish PATH for Actions and fresh SSH shells.
          export PATH="$HOME/.opencode/bin:$PATH"
          printf '%s\n' "$HOME/.opencode/bin" >> "$GITHUB_PATH"
          /usr/bin/time -p sudo -n ln -sfn "$OPENCODE_BIN" /usr/local/bin/opencode
          /usr/bin/time -p opencode --version
          /usr/bin/time -p env PATH=/usr/local/bin:/usr/bin:/bin /bin/bash -c 'command -v opencode; opencode --version'
          install -d -m 700 "$HOME/.config/opencode"
          cat > "$HOME/.config/opencode/opencode.json" <<'EOF'
          {
            "$schema": "https://opencode.ai/config.json",
            "plugin": [],
            "permission": {
              "*": "allow",
              "task": "allow"
            }
          }
          EOF
          if [[ "$OMO_REQUEST" == 'true' && "$RALPH_REQUEST" != 'true' ]]; then
            echo 'Installing oh-my-openagent for the omo-labeled request'
            if ! command -v bun >/dev/null 2>&1; then
              curl --fail --silent --show-error --location https://bun.sh/install | bash
            fi
            export BUN_INSTALL="${BUN_INSTALL:-$HOME/.bun}"
            export PATH="$BUN_INSTALL/bin:$PATH"
            command -v bun
            bunx oh-my-openagent install \
              --no-tui \
              --platform=opencode \
              --claude=no \
              --openai=no \
              --gemini=no \
              --copilot=no \
              --opencode-zen=no \
              --zai-coding-plan=no \
              --opencode-go=no \
              --kimi-for-coding=no \
              --bailian-coding-plan=no \
              --minimax-cn-coding-plan=no \
              --minimax-coding-plan=no \
              --vercel-ai-gateway=no \
              --skip-auth
          fi
          config_tmp="$OPENCODE_WEB_DIR/opencode.json"
          if [[ "$RALPH_REQUEST" == 'true' ]]; then
            npm install --prefix "$HOME/.config/opencode" --no-save --no-package-lock \
              --ignore-scripts opencode-ralph-loop@1.0.10
            jq '
              .plugin = ((.plugin // []) + ["opencode-ralph-loop"]) |
              .permission = ((.permission // {}) + {"*": "allow", "task": "allow"})
            ' "$HOME/.config/opencode/opencode.json" > "$config_tmp"
          elif [[ "$OMO_REQUEST" != 'true' ]]; then
            npm install --prefix "$HOME/.config/opencode" --no-save --no-package-lock \
              --ignore-scripts opencode-goal-plugin@0.8.2
            jq '
              .plugin = ((.plugin // []) + [["opencode-goal-plugin", {"noInterruptOnUserMessage": true}]]) |
              .command.goal = {
                "description": "Set a session-scoped goal and auto-continue until complete.",
                "template": "$ARGUMENTS",
                "agent": "build"
              } |
              .permission = ((.permission // {}) + {"*": "allow", "task": "allow"})
            ' "$HOME/.config/opencode/opencode.json" > "$config_tmp"
          else
            jq '.permission = ((.permission // {}) + {"*": "allow", "task": "allow"})' \
              "$HOME/.config/opencode/opencode.json" > "$config_tmp"
          fi
          mv "$config_tmp" "$HOME/.config/opencode/opencode.json"
          PORTS="$(node --input-type=module -e '
            import net from "node:net";
            const ports = [];
            for (let index = 0; index < 3; index += 1) {
              const server = net.createServer();
              await new Promise((resolve, reject) => {
                server.once("error", reject);
                server.listen(0, "127.0.0.1", resolve);
              });
              ports.push(server.address().port);
              server.close();
            }
            console.log(ports.join(" "));
          ')"
          read -r OPENCODE_WEB_PORT PROJECT_FILE_PORT OPENCODE_CONTROL_PORT <<< "$PORTS"
          [[ "$OPENCODE_WEB_PORT" =~ ^[0-9]+$ && "$PROJECT_FILE_PORT" =~ ^[0-9]+$ ]]
          {
            printf 'OPENCODE_WEB_PORT=%s\n' "$OPENCODE_WEB_PORT"
            printf 'PROJECT_FILE_PORT=%s\n' "$PROJECT_FILE_PORT"
            printf 'OPENCODE_CONTROL_PORT=%s\n' "$OPENCODE_CONTROL_PORT"
          } >> "$GITHUB_ENV"
          echo "Allocated OpenCode port: $OPENCODE_WEB_PORT"
          echo "Allocated project file server port: $PROJECT_FILE_PORT"
          nohup "$OPENCODE_BIN" serve --hostname 127.0.0.1 --port "$OPENCODE_WEB_PORT" \
            > "$OPENCODE_WEB_DIR/opencode.log" 2>&1 &
          echo $! > "$OPENCODE_WEB_DIR/opencode.pid"
          wait_for_http() {
            local url="$1"
            local attempts="$2"
            local response_code
            for _ in $(seq 1 "$attempts"); do
              response_code="$(curl --silent --show-error --max-time 3 \
                --output /dev/null --write-out '%{http_code}' "$url" || true)"
              if [[ "$response_code" =~ ^2[0-9][0-9]$ ]]; then
                return 0
              fi
              sleep 1
            done
            return 1
          }
          wait_for_http_response() {
            local url="$1"
            local attempts="$2"
            local response_code
            for _ in $(seq 1 "$attempts"); do
              response_code="$(curl --silent --show-error --max-time 3 \
                --output /dev/null --write-out '%{http_code}' "$url" || true)"
              if [[ "$response_code" =~ ^[1-5][0-9][0-9]$ ]]; then
                return 0
              fi
              sleep 1
            done
            return 1
          }
          if ! wait_for_http "http://127.0.0.1:$OPENCODE_WEB_PORT/global/health" 45; then
            echo 'OpenCode server did not become healthy; server log follows:' >&2
            curl --silent --show-error --max-time 10 \
              -i "http://127.0.0.1:$OPENCODE_WEB_PORT/global/health" >&2 || true
            sed -n '1,160p' "$OPENCODE_WEB_DIR/opencode.log" >&2 || true
            exit 1
          fi
          if ! command -v nginx >/dev/null 2>&1; then
            case "$RUNNER_OS" in
              Linux)
                sudo apt-get update
                sudo apt-get install -y nginx
                ;;
              macOS)
                brew install nginx
                ;;
              *)
                echo "Nginx is not installed and cannot be provisioned on $RUNNER_OS" >&2
                exit 1
                ;;
            esac
          fi
          NGINX_BIN="$(command -v nginx)"
          NGINX_PREFIX="$OPENCODE_WEB_DIR/nginx"
          install -d -m 700 "$NGINX_PREFIX" "$NGINX_PREFIX/client_body_temp" \
            "$NGINX_PREFIX/proxy_temp" "$NGINX_PREFIX/fastcgi_temp" \
            "$NGINX_PREFIX/uwsgi_temp" "$NGINX_PREFIX/scgi_temp"
          NGINX_CONF_PATH="$("$NGINX_BIN" -V 2>&1 | sed -n 's/.*--conf-path=\([^ ]*\).*/\1/p')"
          /usr/bin/time -p node "$RUNTIME_DIR/scripts/project-file-mime.mjs" \
            "$(dirname "$NGINX_CONF_PATH")/mime.types" "$NGINX_PREFIX/mime.types"
          cat > "$NGINX_PREFIX/nginx.conf" <<EOF
          worker_processes 1;
          pid $NGINX_PREFIX/nginx.pid;
          error_log $NGINX_PREFIX/error.log;
          events { worker_connections 128; }
          http {
            include "$NGINX_PREFIX/mime.types";
            default_type application/octet-stream;
            access_log $NGINX_PREFIX/access.log;
            client_body_temp_path $NGINX_PREFIX/client_body_temp;
            proxy_temp_path $NGINX_PREFIX/proxy_temp;
            fastcgi_temp_path $NGINX_PREFIX/fastcgi_temp;
            uwsgi_temp_path $NGINX_PREFIX/uwsgi_temp;
            scgi_temp_path $NGINX_PREFIX/scgi_temp;
            server {
              listen 127.0.0.1:$PROJECT_FILE_PORT;
              root $PROJECT_DIR;
              index __codex_no_automatic_index_file__;
              add_header Cache-Control "no-cache" always;
              autoindex on;
            }
          }
          EOF
          "$NGINX_BIN" -t -c "$NGINX_PREFIX/nginx.conf" -p "$NGINX_PREFIX"
          "$NGINX_BIN" -c "$NGINX_PREFIX/nginx.conf" -p "$NGINX_PREFIX"
          if ! wait_for_http "http://127.0.0.1:$PROJECT_FILE_PORT/" 15; then
            echo 'Nginx project file server did not become healthy; server log follows:' >&2
            sed -n '1,160p' "$NGINX_PREFIX/error.log" >&2 || true
            exit 1
          fi
          echo 'Cloudflare will tunnel directly to the workspace-scoped OpenCode server and project file server'
          case "$RUNNER_OS/$RUNNER_ARCH" in
            Linux/X64) CLOUDFLARED_ASSET='cloudflared-linux-amd64' ;;
            Linux/ARM64) CLOUDFLARED_ASSET='cloudflared-linux-arm64' ;;
            macOS/X64) CLOUDFLARED_ASSET='cloudflared-darwin-amd64.tgz' ;;
            macOS/ARM64) CLOUDFLARED_ASSET='cloudflared-darwin-arm64.tgz' ;;
            *) echo "Unsupported runner platform: $RUNNER_OS/$RUNNER_ARCH" >&2; exit 1 ;;
          esac
          if [[ "$CLOUDFLARED_ASSET" == *.tgz ]]; then
            curl --fail --silent --show-error --location --output "$OPENCODE_WEB_DIR/cloudflared.tgz" \
              "https://github.com/cloudflare/cloudflared/releases/latest/download/$CLOUDFLARED_ASSET"
            tar -xzf "$OPENCODE_WEB_DIR/cloudflared.tgz" -C "$OPENCODE_WEB_DIR" cloudflared
          else
            curl --fail --silent --show-error --location --output "$OPENCODE_WEB_DIR/cloudflared" \
              "https://github.com/cloudflare/cloudflared/releases/latest/download/$CLOUDFLARED_ASSET"
          fi
          chmod 755 "$OPENCODE_WEB_DIR/cloudflared"
          export OPENCODE_CONTROL_PORT OPENCODE_WEB_PORT
          export GH_TOKEN='${{ github.token }}'
          nohup node "$RUNTIME_DIR/scripts/session-lifecycle.mjs" > "$OPENCODE_WEB_DIR/lifecycle.log" 2>&1 &
          printf '%s\n' "$!" > "$OPENCODE_WEB_DIR/lifecycle.pid"
          nohup "$OPENCODE_WEB_DIR/cloudflared" tunnel --no-autoupdate --url "http://127.0.0.1:$OPENCODE_CONTROL_PORT" \
            > "$OPENCODE_WEB_DIR/cloudflared.log" 2>&1 &
          echo $! > "$OPENCODE_WEB_DIR/cloudflared.pid"
          nohup "$OPENCODE_WEB_DIR/cloudflared" tunnel --no-autoupdate --url "http://127.0.0.1:$APP_PORT" \
            > "$OPENCODE_WEB_DIR/app-cloudflared.log" 2>&1 &
          echo $! > "$OPENCODE_WEB_DIR/app-cloudflared.pid"
          web_retries=0; app_retries=0
          for _ in {1..90}; do
            for tunnel in web app; do
              case "$tunnel" in
                web) prefix="cloudflared"; port="$OPENCODE_CONTROL_PORT"; retries="$web_retries" ;;
                app) prefix="app-cloudflared"; port="$APP_PORT"; retries="$app_retries" ;;
              esac
              if ! kill -0 "$(cat "$OPENCODE_WEB_DIR/$prefix.pid")" 2>/dev/null && [[ "$retries" -lt 2 ]]; then
                /usr/bin/time -p sleep 2
                nohup "$OPENCODE_WEB_DIR/cloudflared" tunnel --no-autoupdate --url "http://127.0.0.1:$port" > "$OPENCODE_WEB_DIR/$prefix.log" 2>&1 &
                printf '%s\n' "$!" > "$OPENCODE_WEB_DIR/$prefix.pid"
                case "$tunnel" in
                  web) web_retries=$((web_retries + 1)) ;;
                  app) app_retries=$((app_retries + 1)) ;;
                esac
              fi
            done
            WEB_URL="$(sed -nE 's#^[^|]*\|[[:space:]]*(https://[a-z0-9-]+\.trycloudflare\.com)[[:space:]]*\|.*$#\1#p' "$OPENCODE_WEB_DIR/cloudflared.log" | head -n 1)"
            APP_URL="$(sed -nE 's#^[^|]*\|[[:space:]]*(https://[a-z0-9-]+\.trycloudflare\.com)[[:space:]]*\|.*$#\1#p' "$OPENCODE_WEB_DIR/app-cloudflared.log" | head -n 1)"
            [[ -n "$WEB_URL" && -n "$APP_URL" ]] && break
            sleep 1
          done
          PROJECT_URL="$WEB_URL/omgithub/files/"
          printf '%s' "$WEB_URL" > "$OPENCODE_WEB_DIR/web-url"
          printf '%s' "$APP_URL" > "$OPENCODE_WEB_DIR/app-url"
          /usr/bin/time -p curl --fail --silent "http://127.0.0.1:$OPENCODE_CONTROL_PORT/omgithub/heartbeat"
          echo "APP_URL=$APP_URL" >> "$GITHUB_ENV"
          printf 'url=%s\nproject_url=%s\n' "$WEB_URL" "$PROJECT_URL" >> "$GITHUB_OUTPUT"
          [[ -n "${WEB_URL:-}" && -n "${PROJECT_URL:-}" && -n "${APP_URL:-}" ]] || { echo '::error::A preview tunnel failed after three attempts.'; exit 1; }

      - name: Configure Git identity
        if: env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        run: |
          git config user.name 'opencode[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'

      - name: Create OpenCode branch
        if: env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/time -p git checkout -B "$BRANCH_NAME" "$TARGET_SHA"

      - name: Generate mock OpenCode project
        if: env.TEST_REQUEST == 'true'
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/time -p install -d "$PROJECT_DIR"
          /usr/bin/time -p cp -R "$RUNTIME_DIR/.github/fixtures/test-project/." "$PROJECT_DIR/"
          /usr/bin/time -p node "$PROJECT_DIR/scripts/write-build-metadata.mjs" \
            "$PROJECT_DIR/build.json" "$GITHUB_RUN_ID" "$TRIGGER_ISSUE_NUMBER" "$TARGET_REPOSITORY"
          /usr/bin/time -p test -f "$PROJECT_DIR/index.html"
          /usr/bin/time -p test -f "$PROJECT_DIR/package.json"
          /usr/bin/time -p install -d "$OPENCODE_WEB_DIR"
          /usr/bin/time -p cp "$RUNTIME_DIR/.github/fixtures/test-project-start.sh" "$PROJECT_DIR/start.sh"

      - name: Resolve OpenCode model and skill labels
        id: opencode_labels
        if: env.TEST_REQUEST != 'true' && env.SSH_ONLY_REQUEST != 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ github.token }}
          script: |
            const number = Number(process.env.TRIGGER_ISSUE_NUMBER);
            const response = await github.rest.issues.listLabelsOnIssue({
              owner: process.env.TARGET_OWNER,
              repo: process.env.TARGET_REPO,
              issue_number: number,
              sort: 'created',
              direction: 'asc',
              per_page: 100
            });
            const names = response.data.map(({ name }) => name);
            const modelLabels = names.filter((name) => name.startsWith('model/'));
            if (modelLabels.length > 1) {
              core.setFailed(`Expected at most one model/* label, found: ${modelLabels.join(', ')}`);
              return;
            }
            const model = modelLabels[0]?.slice('model/'.length) || 'opencode/muse-spark-1.3-contributor-free';
            const skills = names.filter((name) => name.startsWith('skill/'))
              .map((name) => name.slice('skill/'.length)).sort();
            core.setOutput('model', model);
            core.setOutput('skills_json', JSON.stringify(skills));
            core.info(`Resolved model=${model}; skills=${skills.join(', ') || '(none)'}`);

      - name: Restore saved OpenCode conversation
        if: env.RESUME_CHECKPOINT_FILE != '' && env.TEST_REQUEST != 'true' && env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        working-directory: ${{ env.PROJECT_DIR }}
        run: /usr/bin/time -p node "$RUNTIME_DIR/scripts/session-checkpoint.mjs" restore

      - name: Initialize project worker
        if: env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          set -euo pipefail
          /usr/bin/time -p node "$RUNTIME_DIR/scripts/provision-project.mjs"
          /usr/bin/time -p bash -n startup.sh
          /usr/bin/time -p bash startup.sh

      - name: Publish restored workspace
        if: env.RESUME_SESSION_ID != '' && env.TEST_REQUEST != 'true' && env.SSH_ONLY_REQUEST != 'true'
        uses: actions/github-script@v7
        env:
          OPENCODE_WEB_URL: ${{ steps.opencode_web.outputs.url }}
          PROJECT_FILE_URL: ${{ steps.opencode_web.outputs.project_url }}
        with:
          github-token: ${{ github.token }}
          script: |
            const fs = require('node:fs');
            const path = require('node:path');
            const body = [
              `OpenCode Web UI: ${process.env.OPENCODE_WEB_URL}/${Buffer.from(process.env.PROJECT_DIR).toString('base64url')}/session/${process.env.RESUME_SESSION_ID}`,
              `Project files: ${process.env.PROJECT_FILE_URL}`
            ].join('\n');
            await github.rest.issues.createComment({ owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: Number(process.env.TRIGGER_ISSUE_NUMBER), body });

      - name: Start saved app before OpenCode
        id: resume_app
        if: env.RESUME_SESSION_ID != '' && env.TEST_REQUEST != 'true' && env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          set -euo pipefail
          if [[ ! -f start.sh ]]; then
            echo 'Saved project has no start.sh. Run verification after the user request.'
            echo 'RESUME_STARTUP_STATUS=missing' >> "$GITHUB_ENV"
          elif /usr/bin/time -p bash "$RUNTIME_DIR/scripts/start-project.sh"; then
            echo 'RESUME_STARTUP_STATUS=ready' >> "$GITHUB_ENV"
            echo 'ready=true' >> "$GITHUB_OUTPUT"
          else
            echo 'Saved startup failed. Run verification after the user request.'
            echo 'RESUME_STARTUP_STATUS=failed' >> "$GITHUB_ENV"
          fi

      - name: Publish restored preview
        if: steps.resume_app.outputs.ready == 'true'
        uses: actions/github-script@v7
        env:
          OPENCODE_WEB_URL: ${{ steps.opencode_web.outputs.url }}
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.issues.createComment({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: Number(process.env.TRIGGER_ISSUE_NUMBER),
              body: `OpenCode Web UI: ${process.env.OPENCODE_WEB_URL}/${Buffer.from(process.env.PROJECT_DIR).toString('base64url')}/session/${process.env.RESUME_SESSION_ID}\nPlayable preview: ${process.env.APP_URL}\n\nRestored app is ready. New changes are not yet verified.`
            });

      - name: Run OpenCode and locate its web session
        id: opencode_run
        if: env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        working-directory: ${{ env.PROJECT_DIR }}
        env:
          GITHUB_TOKEN: ${{ github.token }}
          OPENCODE_WEB_URL: ${{ steps.opencode_web.outputs.url }}
          OPENCODE_MODEL: ${{ steps.opencode_labels.outputs.model }}
          OPENCODE_SKILLS: ${{ steps.opencode_labels.outputs.skills_json }}
          GOAL_REQUEST: ${{ env.GOAL_REQUEST }}
          RALPH_REQUEST: ${{ env.RALPH_REQUEST }}
          OMO_REQUEST: ${{ env.OMO_REQUEST }}
        run: |
          set -euo pipefail
          if [[ "$TEST_REQUEST" == 'true' ]]; then
            /usr/bin/time -p node "$RUNTIME_DIR/scripts/seed-test-session.mjs"
            RESUME_SESSION_ID="$(cat "$OPENCODE_WEB_DIR/synthetic-session-id")"
            printf '%s\n' fixture > "$OPENCODE_WEB_DIR/main-model"
          else
          OPENCODE_BIN="$HOME/.opencode/bin/opencode"
          PROMPT_FILE="$OPENCODE_WEB_DIR/prompt.txt"
          RUN_LOG="$OPENCODE_WEB_DIR/opencode-run.log"
          RUN_EXIT="$OPENCODE_WEB_DIR/opencode-run.exit"
          printf '%s\n' "$OPENCODE_MODEL" > "$OPENCODE_WEB_DIR/main-model"
          if [[ -n "${RESUME_SESSION_ID:-}" ]]; then
            printf '%s' "$COMMENT_BODY" > "$PROMPT_FILE"
          else
            skill_text="$(jq -r 'if length == 0 then "(none)" else join(", ") end' <<<"$OPENCODE_SKILLS")"
            prompt_template="$(< "$RUNTIME_DIR/.github/prompts/01-build.md")"
            request_text="$COMMENT_BODY"
            prompt="${prompt_template//@COMMENT_BODY@/$request_text}"
            prompt="${prompt//@OPENCODE_MODEL@/$OPENCODE_MODEL}"
            prompt="${prompt//@SKILL_TEXT@/$skill_text}"
            if [[ "$OMO_REQUEST" == 'true' && "$RALPH_REQUEST" != 'true' ]]; then
              prompt=$'ulw\n\n'"$prompt"
            fi
            printf '%s\n' "$prompt" > "$PROMPT_FILE"
            if [[ "$OMO_REQUEST" == 'true' && "$RALPH_REQUEST" != 'true' ]]; then
              cat >> "$PROMPT_FILE" <<'EOF'

              This request has the `omo` issue label. Use oh-my-openagent Ultimate's native Goal system for the persistent objective and `ulw` for full OMO orchestration. Keep the objective active through verified implementation iterations and do not stop after the first successful pass. Preserve the workflow's evidence, commit, and push requirements.
          EOF
            fi
            if [[ "$OMO_REQUEST" == 'true' && "$RALPH_REQUEST" != 'true' ]]; then
              echo 'OpenCode invocation mode: omo-goal-ulw'
            elif [[ "$RALPH_REQUEST" == 'true' ]]; then
              echo 'OpenCode invocation mode: ralph-loop'
            else
              echo "OpenCode invocation mode: $([[ "$GOAL_REQUEST" == 'true' ]] && echo goal || echo standard)"
            fi
          fi
          # The inner shell expands the positional parameters after nohup starts.
          # shellcheck disable=SC2016
          nohup bash -c '
            resume_args=()
            [[ -n "${RESUME_SESSION_ID:-}" ]] && resume_args=(--session "$RESUME_SESSION_ID")
            if [[ -n "${RESUME_SESSION_ID:-}" ]]; then
              exact_prompt="$(cat "$3"; printf .)"
              exact_prompt="${exact_prompt%.}"
              "$1" run --session "$RESUME_SESSION_ID" --auto --dangerously-skip-permissions --attach "http://127.0.0.1:$2" --dir "$PROJECT_DIR" --model "$6" "$exact_prompt" > "$4" 2>&1
            elif [[ "$9" == true ]]; then
              "$1" run "${resume_args[@]}" --auto --dangerously-skip-permissions --attach "http://127.0.0.1:$2" --dir "$PROJECT_DIR" --model "$6" --command ralph-loop "$(cat "$3")" > "$4" 2>&1
            elif [[ "$8" == true ]]; then
              "$1" run "${resume_args[@]}" --auto --dangerously-skip-permissions --attach "http://127.0.0.1:$2" --dir "$PROJECT_DIR" --model "$6" --command goal "$(cat "$3")" > "$4" 2>&1
            elif [[ "$7" == true ]]; then
              "$1" run "${resume_args[@]}" --auto --dangerously-skip-permissions --attach "http://127.0.0.1:$2" --dir "$PROJECT_DIR" --model "$6" --command goal "$(cat "$3")" > "$4" 2>&1
            else
              "$1" run "${resume_args[@]}" --auto --dangerously-skip-permissions --attach "http://127.0.0.1:$2" --dir "$PROJECT_DIR" --model "$6" "$(cat "$3")" > "$4" 2>&1
            fi
            code=$?
            echo "$code" > "$5"
            exit "$code"
          ' \
            _ "$OPENCODE_BIN" "$OPENCODE_WEB_PORT" "$PROMPT_FILE" "$RUN_LOG" "$RUN_EXIT" "$OPENCODE_MODEL" "$GOAL_REQUEST" "$OMO_REQUEST" "$RALPH_REQUEST" \
            > /dev/null 2>&1 &
          echo $! > "$OPENCODE_WEB_DIR/opencode-run.pid"
          fi
          SESSION_ID="${RESUME_SESSION_ID:-}"
          for _ in {1..120}; do
            [[ "$SESSION_ID" == ses_* ]] && break
            SESSION_ID="$(curl --fail --silent --show-error \
              -H "x-opencode-directory: $PROJECT_DIR" \
              "http://127.0.0.1:$OPENCODE_WEB_PORT/session" | jq -r '.[0].id // empty')"
            [[ "$SESSION_ID" == ses_* ]] && break
            sleep 1
          done
          [[ "$SESSION_ID" == ses_* ]]
          /usr/bin/time -p printf '%s\n' "$SESSION_ID" > "$OPENCODE_WEB_DIR/checkpoint-session-id"
          /usr/bin/time -p curl --fail --silent "http://127.0.0.1:$OPENCODE_CONTROL_PORT/omgithub/heartbeat"
          /usr/bin/time -p curl --fail --silent "http://127.0.0.1:$OPENCODE_CONTROL_PORT/omgithub/reconcile"
          DIRECTORY_B64="$(printf '%s' "$PROJECT_DIR" | base64 | tr -d '\n' | tr '+/' '-_' | tr -d '=')"
          printf 'session_id=%s\nsession_url=%s\n' "$SESSION_ID" \
            "${OPENCODE_WEB_URL}/${DIRECTORY_B64}/session/${SESSION_ID}" >> "$GITHUB_OUTPUT"


      - name: Post temporary access details
        id: access_comment
        if: env.SSH_ONLY_REQUEST != 'true'
        uses: actions/github-script@v7
        env:
          AGENTSWEB_SSH_ENABLED: ${{ env.AGENTSWEB_SSH_ENABLED }}
          AGENTSWEB_SSH_READY: ${{ steps.agentsweb.outcome == 'success' }}
          SSH_ACCESS_FILE: ${{ env.SSH_DIR }}/access.env
          SSH_TUNNEL_LOG: ${{ env.SSH_DIR }}/tunnel.log
          OPENCODE_WEB_URL: ${{ steps.opencode_run.outputs.session_url }}
          PROJECT_FILE_URL: ${{ steps.opencode_web.outputs.project_url }}
          PROGRESS_COMMENT_TEMPLATE: ${{ env.RUNTIME_DIR }}/.github/scripts/opencode-progress-comment-template.md
          GOAL_REQUEST: ${{ env.GOAL_REQUEST }}
          RALPH_REQUEST: ${{ env.RALPH_REQUEST }}
        with:
          github-token: ${{ github.token }}
          script: |
            const fs = require('node:fs');
            const sshReady = process.env.AGENTSWEB_SSH_ENABLED === 'true' &&
              process.env.AGENTSWEB_SSH_READY === 'true' &&
              fs.existsSync(process.env.SSH_ACCESS_FILE);
            const access = sshReady
              ? Object.fromEntries(fs.readFileSync(process.env.SSH_ACCESS_FILE, 'utf8').trim().split('\n').map((line) => {
                  const separator = line.indexOf('=');
                  return [line.slice(0, separator), line.slice(separator + 1)];
                }))
              : {};
            const tunnelLog = fs.existsSync(process.env.SSH_TUNNEL_LOG)
              ? fs.readFileSync(process.env.SSH_TUNNEL_LOG, 'utf8')
              : '';
            const tunnelUrl = access.AGENTSWEB_TUNNEL_URL ||
              (tunnelLog.match(/^ssh:\/\/[^\s]+/m) || [])[0] ||
              'not registered';
            const { renderProgressComment } = require(`${process.env.RUNTIME_DIR}/.github/scripts/opencode-comment-templates.cjs`);
            const number = Number(process.env.TRIGGER_ISSUE_NUMBER);
            const run = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const command = process.env.TEST_REQUEST === 'true'
              ? 'mock OpenCode'
              : (process.env.RALPH_REQUEST === 'true'
              ? 'OpenCode with Ralph Loop'
              : (process.env.GOAL_REQUEST === 'true' ? 'OpenCode with Goal' : 'OpenCode'));
            const sshSection = sshReady
              ? [
                  '🔐 **Temporary AgentsWeb SSH session is ready.**', '',
                  `Run: ${run}`, `SSH URL: \`${tunnelUrl}\``, `Host: \`${access.AGENTSWEB_HOST}:${access.AGENTSWEB_PORT}\``, '',
                  'Run this command while the job is active:', '',
                  '```sh', access.AGENTSWEB_COMMAND, '```'
                ].join('\n')
              : (process.env.AGENTSWEB_SSH_ENABLED === 'true'
                ? ['⚠️ **AgentsWeb SSH tunnel setup failed.**', '', `Run: ${run}`, `SSH URL: \`${tunnelUrl}\``].join('\n')
                : '');
            const body = renderProgressComment({
              SSH_SECTION: sshSection,
              UPDATED: new Date().toISOString().replace('T', ' ').replace('Z', ' UTC'),
              OPENCODE_WEB_URL: process.env.OPENCODE_WEB_URL,
              PROJECT_FILE_URL: process.env.PROJECT_FILE_URL || 'unavailable',
              VALIDATION_SESSION_SECTION: '',
              PROGRESS_STATS: process.env.TEST_REQUEST === 'true'
                ? '- Elapsed: under 1 minute\n- Token count: 0\n- Speed score: fixture copy\n- Tool calls: 0\n- Active tool calls: 0\n- Active subagents: 0\n- Total subagents executed: 0\n- Total failed subagents: 0\n- Image-context model calls: 0\n- Changed workspace files: fixture project'
                : '- Waiting for live OpenCode progress statistics.',
              ACCESS_NOTE: process.env.TEST_REQUEST === 'true'
                ? 'This is a synthetic OpenCode session for the full workflow test. Generation is replaced only by the fixture copy; reporting and delivery continue normally.'
                : `This opens the currently running ${command} session directly. Access remains available while OpenCode runs and for 5 hours afterwards.`
            });
            const comment = await github.rest.issues.createComment({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO, issue_number: number, body
            });
            core.setOutput('comment_id', comment.data.id);

      - name: Start live OpenCode response comment
        id: response_comment
        if: env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          COMMENT_ID: ${{ steps.access_comment.outputs.comment_id }}
          SESSION_ID: ${{ steps.opencode_run.outputs.session_id }}
          OPENCODE_WEB_URL: ${{ steps.opencode_run.outputs.session_url }}
          PROJECT_FILE_URL: ${{ steps.opencode_web.outputs.project_url }}
          ISSUE_NUMBER: ${{ env.TRIGGER_ISSUE_NUMBER }}
          REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          TRACKER="$OPENCODE_WEB_DIR/response-comment-tracker.sh"
          install -m 700 "$RUNTIME_DIR/.github/scripts/opencode-progress-tracker.sh" "$TRACKER"
          # The tracker is intentionally independent of the step shell so it can poll
          # while later workflow steps run; cleanup stops it at the end of the job.
          nohup env PROJECT_DIR="$PROJECT_DIR" OPENCODE_WEB_PORT="$OPENCODE_WEB_PORT" \
            OPENCODE_WEB_DIR="$OPENCODE_WEB_DIR" SESSION_ID="$SESSION_ID" \
            OPENCODE_WEB_URL="$OPENCODE_WEB_URL" \
            PROJECT_FILE_URL="${{ steps.opencode_web.outputs.project_url }}" \
            PROGRESS_COMMENT_TEMPLATE="$RUNTIME_DIR/.github/scripts/opencode-progress-comment-template.md" \
            AGENTSWEB_SSH_ENABLED="$AGENTSWEB_SSH_ENABLED" \
            AGENTSWEB_SSH_READY="${{ steps.agentsweb.outcome == 'success' }}" \
            SSH_COMMAND="${AGENTSWEB_COMMAND:-}" \
            SSH_HOST="${AGENTSWEB_HOST:-}" \
            SSH_PORT="${AGENTSWEB_PORT:-}" \
            RUN_URL="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" \
            COMMENT_ID="$COMMENT_ID" REPOSITORY="$REPOSITORY" GH_TOKEN="$GH_TOKEN" \
            "$TRACKER" > "$OPENCODE_WEB_DIR/response-comment-tracker.log" 2>&1 &
          echo $! > "$OPENCODE_WEB_DIR/response-comment-tracker.pid"
          sleep 1
          if ! kill -0 "$(cat "$OPENCODE_WEB_DIR/response-comment-tracker.pid")" 2>/dev/null; then
            cat "$OPENCODE_WEB_DIR/response-comment-tracker.log" >&2
            exit 1
          fi

      - name: Verify Ralph completion promise
        if: env.TEST_REQUEST != 'true' && env.SSH_ONLY_REQUEST != 'true' && env.RALPH_REQUEST == 'true' && env.RESUME_SESSION_ID == ''
        shell: bash
        working-directory: ${{ env.PROJECT_DIR }}
        env:
          SESSION_ID: ${{ steps.opencode_run.outputs.session_id }}
        run: |
          set -euo pipefail
          RUN_EXIT="$OPENCODE_WEB_DIR/opencode-run.exit"
          RUN_LOG="$OPENCODE_WEB_DIR/opencode-run.log"
          RESPONSE_FILE="$OPENCODE_WEB_DIR/ralph-response.json"
          for _ in {1..21600}; do
            [[ -f "$RUN_EXIT" ]] && break
            sleep 1
          done
          [[ -f "$RUN_EXIT" ]]
          code="$(cat "$RUN_EXIT")"
          [[ "$code" == 0 ]]
          curl --fail --silent --show-error \
            -H "x-opencode-directory: $PROJECT_DIR" \
            "http://127.0.0.1:$OPENCODE_WEB_PORT/session/$SESSION_ID/message" \
            > "$RESPONSE_FILE"
          response_text="$(jq -r '[.[] | select(.info.role == "assistant") | (.parts[]? | select(.type == "text") | .text)] | join("\n")' "$RESPONSE_FILE")"
          if grep -Eiq '<promise>DONE</promise>' "$RUN_LOG" || grep -Eiq '<promise>DONE</promise>' <<<"$response_text"; then
            echo 'Ralph completion promise verified.'
          else
            echo 'Ralph stopped without <promise>DONE</promise>; refusing completion.' >&2
            exit 1
          fi

      - name: Wait for main OpenCode completion
        if: env.TEST_REQUEST != 'true' && env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        run: |
          set -euo pipefail
          for _ in {1..21600}; do
            [[ -f "$OPENCODE_WEB_DIR/opencode-run.exit" ]] && break
            /usr/bin/time -p sleep 1
          done
          [[ -f "$OPENCODE_WEB_DIR/opencode-run.exit" ]]
          [[ "$(cat "$OPENCODE_WEB_DIR/opencode-run.exit")" == 0 ]]

      - name: Verify synthetic production delivery
        if: env.TEST_REQUEST == 'true' && env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/time -p node "$RUNTIME_DIR/scripts/wait-deployment.mjs"

      - name: Create OpenCode logs release
        continue-on-error: true
        if: always() && env.SSH_ONLY_REQUEST != 'true' && steps.opencode_run.outcome == 'success'
        shell: bash
        run: |
          set -euo pipefail
          release_dir="$RUNNER_TEMP/opencode-release"
          mkdir -p "$release_dir"

          # Preserve machine-readable responses and execution logs without
          # publishing OpenCode configuration, credentials, or SSH material.
          /usr/bin/time -p node "$RUNTIME_DIR/scripts/log-response.mjs" "$release_dir/opencode-response.json"
          if [[ ! -s "$release_dir/opencode-response.json" ]]; then
            echo 'OpenCode response artifact is empty; refusing to create an incomplete release.' >&2
            exit 1
          fi
          for artifact in "$OPENCODE_WEB_DIR"/*.log; do
            [[ -f "$artifact" && -s "$artifact" ]] || continue
            [[ "$(basename "$artifact")" == 'opencode.json' ]] && continue
            cp -f "$artifact" "$release_dir/"
          done

          release_assets=()
          for asset in "$release_dir"/*; do
            [[ -f "$asset" && -s "$asset" ]] || continue
            release_assets+=("$asset")
          done
          ((${#release_assets[@]} > 0))

          tag="opencode-logs-${GITHUB_RUN_ID}"
          gh release create "$tag" "${release_assets[@]}" \
            --target "$TARGET_SHA" \
            --title "OpenCode logs for run #${GITHUB_RUN_NUMBER}" \
            --notes "Generated OpenCode response and execution logs for workflow run [#${GITHUB_RUN_NUMBER}](https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID})." \
            --latest=false

        env:
          GH_TOKEN: ${{ github.token }}

      - name: Mark issue complete
        if: success() && env.TEST_REQUEST == 'true' && env.SSH_ONLY_REQUEST != 'true'
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          retries: 3
          github-token: ${{ github.token }}
          script: |
            const number = Number(process.env.TRIGGER_ISSUE_NUMBER);
            await github.rest.issues.removeLabel({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: number, name: 'in progress'
            }).catch((error) => { if (error.status !== 404) throw error; });
            await github.rest.issues.removeLabel({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: number, name: 'validating'
            }).catch((error) => { if (error.status !== 404) throw error; });
            await github.rest.issues.addLabels({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: number, labels: ['complete']
            });

      - name: Mark issue failed
        if: failure()
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          retries: 3
          github-token: ${{ github.token }}
          script: |
            const number = Number(process.env.TRIGGER_ISSUE_NUMBER);
            const runUrl = `https://github.com/${process.env.TARGET_REPOSITORY}/actions/runs/${context.runId}`;
            const warn = (error) => core.warning(error.message);
            await github.rest.issues.createComment({
              owner: process.env.TARGET_OWNER,
              repo: process.env.TARGET_REPO,
              issue_number: number,
              body: `Build failed. [Open workflow logs](${runUrl}).`
            }).catch(warn);
            await github.rest.issues.removeLabel({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: number, name: 'in progress'
            }).catch((error) => { if (error.status !== 404) warn(error); });
            await github.rest.issues.removeLabel({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: number, name: 'validating'
            }).catch((error) => { if (error.status !== 404) warn(error); });
            await github.rest.issues.addLabels({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: number, labels: ['failed']
            }).catch(warn);

      - name: Keep temporary access available for 5 hours
        if: (success() || (failure() && vars.OPENCODE_DEBUG_HOLD == 'true' && steps.opencode_web.outcome == 'success')) && env.TEST_REQUEST != 'true' && env.SSH_ONLY_REQUEST != 'true'
        shell: bash
        run: sleep 18000

      - name: Stop lifecycle controller
        if: always() && env.SSH_ONLY_REQUEST != 'true' && steps.opencode_run.outcome == 'success'
        shell: bash
        run: |
          set -euo pipefail
          if [[ -f "$OPENCODE_WEB_DIR/lifecycle.pid" ]]; then
            /usr/bin/time -p kill "$(cat "$OPENCODE_WEB_DIR/lifecycle.pid")" 2>/dev/null || true
            for _ in {1..130}; do
              kill -0 "$(cat "$OPENCODE_WEB_DIR/lifecycle.pid")" 2>/dev/null || exit 0
              /usr/bin/time -p sleep 1
            done
            exit 1
          fi
          echo 'Lifecycle controller was not started.'

      - name: Append SSH session closed
        if: always() && env.AGENTSWEB_SSH_ENABLED == 'true' && steps.agentsweb.outcome == 'success' && (steps.agentsweb.outputs.comment_id != '' || steps.access_comment.outputs.comment_id != '' || steps.ssh_access_comment.outputs.comment_id != '')
        uses: actions/github-script@v7
        with:
          github-token: ${{ github.token }}
          script: |
            const { renderTemplate } = require(`${process.env.RUNTIME_DIR}/.github/scripts/opencode-comment-templates.cjs`);
            await github.rest.issues.createComment({
              owner: process.env.TARGET_OWNER, repo: process.env.TARGET_REPO,
              issue_number: Number(process.env.TRIGGER_ISSUE_NUMBER),
              body: renderTemplate('opencode-ssh-closed-comment.md', {
                RUN_NUMBER: context.runNumber,
                RUN_URL: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
              })
            });

      - name: Clean up OpenCode session
        if: always()
        shell: bash
        run: |
          set +e
          [[ -f "$OPENCODE_WEB_DIR/lifecycle.pid" ]] && kill "$(cat "$OPENCODE_WEB_DIR/lifecycle.pid")" 2>/dev/null || true
          [[ -f "$OPENCODE_WEB_DIR/cloudflared.pid" ]] && kill "$(cat "$OPENCODE_WEB_DIR/cloudflared.pid")" 2>/dev/null || true
          [[ -f "$OPENCODE_WEB_DIR/nginx/nginx.pid" ]] && kill "$(cat "$OPENCODE_WEB_DIR/nginx/nginx.pid")" 2>/dev/null || true
          [[ -f "$OPENCODE_WEB_DIR/opencode.pid" ]] && kill "$(cat "$OPENCODE_WEB_DIR/opencode.pid")" 2>/dev/null || true
          [[ -f "$OPENCODE_WEB_DIR/opencode-run.pid" ]] && kill "$(cat "$OPENCODE_WEB_DIR/opencode-run.pid")" 2>/dev/null || true
          [[ -f "$OPENCODE_WEB_DIR/response-comment-tracker.pid" ]] && kill "$(cat "$OPENCODE_WEB_DIR/response-comment-tracker.pid")" 2>/dev/null || true
          [[ -f "$OPENCODE_WEB_DIR/app-cloudflared.pid" ]] && kill "$(cat "$OPENCODE_WEB_DIR/app-cloudflared.pid")" 2>/dev/null || true
          [[ -f "$OPENCODE_WEB_DIR/app.pid" ]] && kill "$(cat "$OPENCODE_WEB_DIR/app.pid")" 2>/dev/null || true
          tmux kill-session -t app-server 2>/dev/null || true
          [[ -f "$SSH_DIR/tunnel.pid" ]] && kill "$(cat "$SSH_DIR/tunnel.pid")" 2>/dev/null || true
          [[ -f "$SSH_DIR/sshd.pid" ]] && sudo kill "$(cat "$SSH_DIR/sshd.pid")" 2>/dev/null || true
          sudo pkill -f 'sshd.*2222' 2>/dev/null || true
          rm -f "$SSH_KEY" "$SSH_PUBLIC_KEY"
          rm -rf "$SSH_DIR" "$HOME/.ssh/authorized_keys"
          rm -rf "$OPENCODE_WEB_DIR"

      - name: Report terminal build status
        if: always()
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
          OMGITHUB_ORIGIN: ${{ vars.OMGITHUB_ORIGIN || 'https://omgithub.com' }}
          OUTCOME: ${{ job.status }}
        shell: bash
        run: |
          sequence=$(date +%s)000
          /usr/bin/time -p curl --fail-with-body --silent --show-error --retry 2 --retry-all-errors --max-time 15 \
            -H "Authorization: Bearer $OMGITHUB_CALLBACK_TOKEN" -H 'Content-Type: application/json' \
            --data "{\"run\":\"$GITHUB_RUN_ID\",\"attempt\":$GITHUB_RUN_ATTEMPT,\"sequence\":$sequence,\"state\":\"ended\",\"phase\":\"building\",\"outcome\":\"$OUTCOME\"}" \
            "$OMGITHUB_ORIGIN/api/github/$GITHUB_REPOSITORY/issues/$TRIGGER_ISSUE_NUMBER/run"
