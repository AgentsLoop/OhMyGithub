@COMMENT_BODY@

Build the requested game fully, run the unit tests, and work directly on the current branch. Do not create a branch or pull request; the workflow will handle both. Start the app server in a tmux session named app-server, bind it to port 3000, and leave that tmux session running for verification.

Workflow label context: model/@OPENCODE_MODEL@; skills: @SKILL_TEXT@. Use the listed skills when relevant.
