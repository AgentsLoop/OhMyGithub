Deterministic Android verification failed during phase `@FAILED_PHASE@` on attempt @ATTEMPT@ of @MAX_ATTEMPTS@. Inspect `@VERIFICATION_JSON@` and the logs under `@EVIDENCE_DIR@`, identify the root cause, and fix the Android project now. Run the relevant Gradle tests and `assembleRelease` after the fix. The project must produce exactly one release APK, install and launch on the already running API 35 emulator, remain crash-free, support its meaningful interaction flow, and allow a valid live screenshot. Do not merely describe the failure, create a branch, or create a pull request; the workflow handles delivery. A deterministic verification attempt will run again after your repair.
