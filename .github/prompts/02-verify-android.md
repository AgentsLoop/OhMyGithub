Now verify the completed Android app on the already running emulator. Follow `./Agents.md` and do the work rather than only describing it. Run the relevant Gradle tests and release build, install the APK with ADB, launch its declared activity, and exercise meaningful interactive app states. Inspect `adb logcat`, the foreground activity, and the process state for crashes or broken behavior; fix every failure you find and rebuild and reinstall as needed. Capture at least one meaningful screenshot from the live emulator with `adb exec-out screencap -p`, saving it under `screenshots/` with a filename beginning with `final-android-`. Confirm the installed app is working in your completion response. Do not create a branch or pull request; the workflow handles delivery and publication.
