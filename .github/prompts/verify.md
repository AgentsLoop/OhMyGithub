Now verify the completed app. Follow `./Agents.md`, ensure the app server is running in the tmux session named app-server on port 3000, run browser tests, fix any failures, and confirm the app works on port 3000. Capture final browser screenshots under `screenshots/` with filenames beginning with `final-`, and publish the required completion update to the triggering GitHub issue. Do not create a branch or pull request; the workflow will handle both.
