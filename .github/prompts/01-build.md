@COMMENT_BODY@

Create a playable browser entrypoint and a portable `startup.sh` in the project root. Make the script change to its own directory, install required dependencies, build when needed, and serve the app in the foreground on `${PORT:-3000}`. Add per-command timing. Keep source changes in this workspace. Leave screenshots and deployment to the background validator after your response completes.
