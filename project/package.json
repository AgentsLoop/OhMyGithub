{
  "name": "crystal-catcher",
  "version": "1.0.0",
  "description": "Crystal Catcher - Catch falling crystals, avoid bombs!",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host 0.0.0.0",
    "start": "vite --port 3000 --host 0.0.0.0",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
