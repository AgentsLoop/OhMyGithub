{
  "name": "aiplay-smoke-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "start": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
