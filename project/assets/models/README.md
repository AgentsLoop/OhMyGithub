# assets/models

Future GLB models live here.

Path convention: `assets/models/<name>.glb` (and matching `.gltf` if needed).  
Renderer stub `loadGlbPlaceholder` validates this prefix — real loader will use `@babylonjs/loaders` `SceneLoader.ImportMeshAsync`.

No binary assets committed at foundation stage; this folder documents the contract.
