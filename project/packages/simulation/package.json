{
  "name": "@rts/simulation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic fixed-timestep headless simulation — no renderer, no DOM",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@rts/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
