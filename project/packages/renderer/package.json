{
  "name": "@rts/renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Babylon.js renderer — consumes snapshots, never writes simulation state",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@rts/contracts": "workspace:*",
    "@babylonjs/core": "^7.32.0",
    "@babylonjs/loaders": "^7.32.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
