{
  "name": "@rts/simulation-world",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "World scaffolding: scenario registry, placeholder systems (no renderer)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@rts/contracts": "workspace:*",
    "@rts/simulation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
