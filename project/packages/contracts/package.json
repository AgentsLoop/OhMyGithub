{
  "name": "@rts/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency contracts: commands, events, snapshots, worker protocol",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
